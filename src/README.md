# Source

Application source for **AFK Journey Trainer** will live here.

Expected modules:

```
src/
├── attach/       # Process attach + pattern scanning
├── features/     # Num1–Num9 memory patches
├── overlay/      # Insert menu UI + sliders
└── signatures/   # Version-specific pattern database
```
